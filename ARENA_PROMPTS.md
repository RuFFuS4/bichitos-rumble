# Arena Packs — Prompt Library

> **Status**: Pack 1 (Jungle Tropic) shipped 2026-04-23. This doc holds
> the ready-to-paste prompts for the 4 remaining arena packs:
> Frozen Tundra · Desert Dunes · Coral Reef Beach · Kitsune Shrine.
>
> Each pack is 3 things generated separately:
>   - **1 skybox** (panorama 2048×1024, no char limit on prompt).
>   - **1 ground texture** (tileable 1024×1024, no char limit).
>   - **5–6 prop GLBs** (each under the user's 800-char prompt budget).
>
> The arena floor (29 collapsing fragments) stays procedural — props
> live in a ring at radius 14–22 around a circular arena of radius 12.
> A JSON placement file is generated by the game, not by the user.

## Pipeline tip (burned-in lesson from Jungle pack)

The IA generator consistently outputs millions of verts and
per-leaf primitives that resist every merge strategy. **Always add
this line to every prop prompt**, otherwise you risk models in the
60–300 MB range that even the aggressive pipeline can't rescue:

```
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT make each leaf / face / gem a separate mesh. Target < 3000
triangles total. Low-poly chunky shapes. Think "Fall Guys obstacle"
or "Minecraft plant", not photorealistic foliage.
```

The line eats ~200 chars of the 800-char budget — the remaining 600
are plenty for the prop description.

## Common preamble (goes on top of every prop prompt)

Copy-paste this block at the start of every GLB prop prompt:

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, slight cel-shading, NO
realism. Reference mix: Fall Guys + Smash Bros trophy + Pixar.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT make each leaf / face / gem a separate mesh. Target < 3000
triangles total. Low-poly chunky shapes.
Output: GLB, Y-up metres, 1024px PBR baked texture, metalness ≤ 0.3,
no skeleton, no embedded lighting. Origin at base centre, Y=0.
```

~430 chars. Leaves ~370 chars for the prop-specific body.

---

# Pack 2 — FROZEN TUNDRA (❄️, Kowalski)

## Files to generate

- `public/images/skyboxes/frozen_tundra.png` (skybox, 2048×1024)
- `public/images/arena-ground/frozen_tundra.png` (ground tile, 1024×1024)
- 6 GLB props (names literal, exact file names):
  - `iceberg_tall.glb`
  - `iceberg_mid.glb`
  - `iceberg_low.glb`
  - `pine_snow.glb`
  - `signpost_wood.glb`
  - `ice_shard.glb`

Palette: glacial ice blue (#8fd8ff → #3a8fc9), bright white (#f5faff),
pale lavender shadow (#c8c0e8), silver-grey rock (#5a6a78), warm
orange accents on the wooden signpost (#f08a3a).

## Skybox prompt (no char limit)

```
Equirectangular 360° panoramic skybox, 2048×1024 px, seamlessly
tileable horizontally (left edge = right edge). Cartoon stylised
arcade game scene.

Subject: Antarctic / arctic night sky with aurora borealis. Looking
outward across a frozen plain. Snow-capped distant mountains line
the horizon at ~20% of image height (muted lavender-blue silhouettes,
no individual ridges in focus).

Sky:
  - Deep navy upper (#0a1530) fading to soft purple-violet near the
    mountains (#6a4a8a).
  - Aurora borealis ribbons crossing the sky: soft, painterly,
    flowing S-curves in luminous green (#6aff9a → #3ad88c) and
    magenta (#e060d0 → #a040a0). Three or four broad bands, not
    realistic photo.
  - Scattered bright stars as tiny cream-white dots (~30 pinpricks
    across the upper half), no constellations.
  - Faint distant misty snow fog on the horizon line.

Style: stylised painterly cartoon. Soft gradients. No photoreal
leaf, grain, or detail. Bold colour regions. Think Disney animated
feature or Pixar matte painting.

Technical: exact 2:1 equirectangular, left edge tileable with
right. No watermark, no characters, no text, no ui. Fully opaque.
```

## Ground texture prompt (no char limit)

```
Seamlessly tileable square texture, 1024×1024 px, cartoon stylised,
flat lighting (no baked shadows).

Subject: Antarctic packed snow floor with subtle ice sheen and
footprints. Mostly smooth snow-white (#f5faff → #e0e8f0) with:
  - Occasional darker grey-blue patches of compacted ice (#c8d8e8).
  - A few fine hairline cracks (#8aa8c8) radiating gently.
  - 2–3 small faded footprints (tiny critter paws), very soft, not
    boot-tread photo detail.
  - Tiny glints: 4–5 pale blue twinkle highlights (#ffffff with
    slight blue tint) scattered, suggesting ice crystals.

Palette: bright snow white, pale blue shadow, muted lavender hint.
Even brightness so the game's runtime lighting tints it. Matte, no
gloss. Perfectly top-down.

Technical: fully seamless / tileable, 1024×1024 PNG, no alpha
channel, no watermark.
```

## Prop prompts (each < 800 chars, copy-paste ready)

### `iceberg_tall.glb` (633 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, slight cel-shading, NO
realism. Reference mix: Fall Guys + Smash Bros trophy + Pixar.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT make each face a separate mesh. Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked texture, metalness ≤ 0.3,
no skeleton, no embedded lighting. Origin at base centre, Y=0.

Generate: TALL JAGGED ICEBERG SPIKE.

Dimensions: 4.0 m tall, base ~1.2 × 1.2 m tapering to a sharp apex.
Faceted angular geometry (crystalline, like cut glass) in glacial
ice blue (#8fd8ff → #3a8fc9), upper half slightly brighter and a
hint translucent. Lower base darker, mossy-ice rim (#4a6a8a).
```

### `iceberg_mid.glb` (580 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, slight cel-shading, NO
realism. Reference mix: Fall Guys + Smash Bros trophy + Pixar.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 3000 triangles. Low-poly chunky shapes.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: MID-SIZE BLOCKY ICEBERG with a FLAT TOP.

Dimensions: 2.5 m tall, 1.8 × 1.8 m base. Boxy chunky shape, faceted
ice blue (#8fd8ff) with white highlights on top edges. Flat top
readable as a tiny platform.
```

### `iceberg_low.glb` (522 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SHORT CHUNKY ICE BLOCK.

Dimensions: 1.2 m tall, ~1.0 m wide. Low-poly blocky shape with
5–7 flat facets, glacial ice blue (#8fd8ff → #6ab8d8), for scatter
placement around larger icebergs.
```

### `pine_snow.glb` (667 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, slight cel-shading, NO
realism. Reference mix: Fall Guys + Smash Bros trophy + Pixar.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT make each branch or needle cluster a separate mesh. Target
< 3000 triangles. Chunky foliage shapes only.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SNOW-LADEN PINE / FIR CONIFER TREE.

Dimensions: 3.8 m tall, ~1.2 m wide canopy. Classic triangular pine
silhouette. Dark green needles (#2a5a3a) mostly covered by chunky
white snow layers on top of each branch tier. Brown trunk base
(#5a3a1a) visible ~0.5 m.
```

### `signpost_wood.glb` (585 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 1500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: LEANING WOODEN SIGNPOST.

Dimensions: 2.0 m tall, leaning ~10°. Warm brown wooden pole
(#8a5a2a) with a blank rectangular wooden plate near the top
(0.8 × 0.4 m). Small snow cap on the plate's top edge. Optional
small orange accent (#f08a3a) on the plate border. No text.
```

### `ice_shard.glb` (505 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 1500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: CLUSTER OF 4–5 CRYSTALLINE ICE SHARDS.

Dimensions: 0.8 m tall, pointing up, varied spike angles. Faceted
crystal shapes, pale ice blue (#8fd8ff), translucent hint on tips.
Clustered at shared base, like frozen upthrust.
```

---

# Pack 3 — DESERT DUNES (🏜️, Sihans)

## Files to generate

- `public/images/skyboxes/desert_dunes.png` (2048×1024)
- `public/images/arena-ground/desert_dunes.png` (1024×1024)
- 7 GLB props:
  - `sandstone_spire_tall.glb`
  - `sandstone_spire_short.glb`
  - `cactus_saguaro.glb`
  - `palm_desert.glb`
  - `minecart_rusted.glb`
  - `bones_skull_scatter.glb`
  - `cloth_flag_tattered.glb`

Palette: warm sand (#e6c178 → #d19b4a), burnt orange (#d35a1a), deep
crimson rocks (#a33030), cactus green (#5a8c4a), pale bone cream
(#f0e8d0), tiny turquoise accents (#40c0b0).

## Skybox prompt (no char limit)

```
Equirectangular 360° panoramic skybox, 2048×1024 px, seamless
horizontal tile.

Subject: golden-hour desert sunset. Looking outward across vast
dunes. Sun is very low, just above the horizon (at ~5% of image
height from bottom). Warm painterly sky with three distinct bands:

  - Upper third: deep violet-purple (#4a2a6a), few high wispy clouds
    in warm pink.
  - Middle third: pink-orange sunset gradient (#f0805a → #f0a070).
  - Lower third near horizon: dusty gold (#f0c870), hazy.

Horizon line: distant sandy dune silhouettes, mildly curved, in a
slightly darker sand tone (#c89a58). No vegetation visible.

Style: stylised painterly cartoon, bold colour regions, no
photoreal cloud detail. Think Pixar matte painting or classic
Disney Aladdin sunset.

Technical: 2:1, left=right tileable, no watermark, no characters,
no text. Fully opaque.
```

## Ground texture prompt (no char limit)

```
Seamlessly tileable square texture, 1024×1024 px, cartoon stylised,
flat lighting.

Subject: fine golden desert sand with wind-rippled patterns. Base
colour warm sand (#e6c178 → #d19b4a). Wind ripples as subtle
parallel waves crossing the tile at a gentle angle, each wave
~80 px wide. A few scattered elements:
  - 3–4 small stones (pebbles) in warm grey-brown (#a88868).
  - 1–2 bleached tiny bone fragments (#f0e8d0).
  - A few barely-visible footprints (critter paws), very soft.

Style: painterly cartoon, even brightness, matte finish. No
individual grain detail. Top-down flat.

Technical: fully seamless / tileable, 1024×1024 PNG, no alpha, no
watermark.
```

## Prop prompts

### `sandstone_spire_tall.glb` (627 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 3000 triangles. Chunky eroded shapes.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: TALL ERODED SANDSTONE PILLAR / HOODOO.

Dimensions: 5.0 m tall, base ~1.2 m wide tapering to ~0.7 m at top.
Faceted wind-carved layers stacked vertically, warm sand to burnt
orange gradient (#e6c178 → #d35a1a). Top slightly tilted ~5° for
character. No vegetation.
```

### `sandstone_spire_short.glb` (515 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SHORT FAT SANDSTONE SPIRE.

Dimensions: 2.5 m tall, base ~1.5 m wide, stout. Faceted layered
erosion. Sand-orange gradient (#d19b4a → #a33030).
```

### `cactus_saguaro.glb` (592 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT split arms or flowers as separate meshes. Target < 2500
triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: CLASSIC SAGUARO CACTUS.

Dimensions: 2.8 m tall. Central trunk with 2–3 curved arms bending
upward, cactus green (#5a8c4a → #6aa84a), vertical ribbed texture
on the skin. Top: 2–3 small magenta flower buds (#e060c0).
```

### `palm_desert.glb` (549 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT split fronds. Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: THIN DESERT DATE PALM.

Dimensions: 3.0 m tall. Slender brown trunk (#8a6a3a), exposed
root ring at the base. 5 wispy drooping fronds, olive green
(#7a9850). Sparser and taller than a tropical palm.
```

### `minecart_rusted.glb` (624 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: RUSTED ABANDONED MINECART on broken rails.

Dimensions: 2.0 × 1.0 × 1.0 m. Wooden frame box cart (weathered
brown, #5a3a1a) with rusted iron bands (#8a4a2a). Four small iron
wheels, leaning slightly tilted as if derailed. Short broken rail
segment under it, rusty orange.
```

### `bones_skull_scatter.glb` (546 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
All bones in ONE mesh (don't separate). Target < 1500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: CLUSTER OF BLEACHED CARTOON BONES.

Dimensions: 0.6 m tall. One cartoon cow-like skull (eye sockets
visible) + 4 rib bones scattered around it. Pale cream-white
(#f0e8d0), slightly dusty.
```

### `cloth_flag_tattered.glb` (563 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: WOODEN POLE WITH TATTERED CLOTH BANNER.

Dimensions: 2.2 m tall pole. Thin wooden pole (#8a5a2a), torn
cloth banner attached at top with diagonal red-orange stripes
(#d35a1a / #f0805a), ragged bottom edge.
```

---

# Pack 4 — CORAL REEF BEACH (🌊, Shelly / Sebastian)

## Files to generate

- `public/images/skyboxes/coral_beach.png` (2048×1024)
- `public/images/arena-ground/coral_beach.png` (1024×1024)
- 8 GLB props:
  - `coral_stack_red.glb`
  - `coral_stack_pink.glb`
  - `coral_brain.glb`
  - `palm_beach_tilted.glb`
  - `shipwreck_hull_piece.glb`
  - `boulder_wet.glb`
  - `seashell_scatter.glb`
  - `starfish_decor.glb`

Palette: turquoise water (#40c0c0 → #70e0d0), wet sand beige
(#e8d0a0), coral red (#e25a3a → #f0805a), coral pink (#f09a9a),
fresh palm green (#3aa24a), white shells.

## Skybox prompt

```
Equirectangular 360° panoramic skybox, 2048×1024 px, seamless
horizontal tile.

Subject: tropical open-ocean horizon at midday. Calm sunny weather.
Horizon at ~45% of image height, splitting sea below from sky above.

Upper half (sky): soft turquoise-blue gradient (#70b8e0 upper fading
to cream #e8f0f0 near horizon). Puffy cumulus clouds in warm
cream-white, 4–6 scattered, low detail.

Lower half (sea): deeper tropical turquoise (#40c0c0 → #3aa8a8), a
subtle sun glint near the centre of the horizon line.

No ships, no islands, no characters, no text. Painterly cartoon,
no photoreal wave detail.

Technical: 2:1, seamless, no watermark.
```

## Ground texture prompt

```
Seamlessly tileable square texture, 1024×1024 px, cartoon stylised,
flat lighting.

Subject: wet tropical beach sand at the shoreline. Base colour wet
sand beige (#e8d0a0 → #d8b878). Slightly darker stripes suggesting
tidal wave line (#b09860).

Scattered details: 5–6 tiny seashells (white-pink), 2–3 small
stones, a few tiny starfish silhouettes (orange), and occasional
soft glints where water pools catch light. Top-down, matte.

Technical: fully seamless / tileable, 1024×1024 PNG, no alpha, no
watermark.
```

## Prop prompts

### `coral_stack_red.glb` (574 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT split branches. Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: RED BRANCHING CORAL FORMATION.

Dimensions: 3.0 m tall, ~1.5 m wide. Organic branching coral in
coral red (#e25a3a → #f0805a) with subtle deeper red veins. Chunky
simplified branch shapes, stubby not delicate.
```

### `coral_stack_pink.glb` (554 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: PINK CABBAGE CORAL.

Dimensions: 2.4 m tall, ~1.2 m wide. Tightly curled cabbage-style
folds, coral pink (#f09a9a → #d86878). Organic lumpy silhouette,
base widens slightly.
```

### `coral_brain.glb` (498 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: BRAIN CORAL ROUND DOME.

Dimensions: 1.2 m diameter, 0.8 m tall. Round half-sphere with
swirly surface grooves. Muted sandstone-pink (#c89090).
```

### `palm_beach_tilted.glb` (620 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT split fronds or coconuts. Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: COCONUT PALM TREE TILTED SIDEWAYS ~45° as if leaning
toward the sea.

Dimensions: 3.5 m long trunk (measured along tilt), warm brown
(#8a5a2a) segmented. Crown at top with 5–6 drooping fronds
(#3aa24a). 3 coconuts clustered.
```

### `shipwreck_hull_piece.glb` (632 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: BROKEN WOODEN SHIP HULL FRAGMENT.

Dimensions: 2.5 × 1.5 × 2.0 m. Curved wooden planks (dark weathered
brown #4a2d14 → #6a3a1a), broken jagged edges, a few iron bolts,
small patches of green-white barnacles clinging. Half-sunk feel,
bottom edge ragged.
```

### `boulder_wet.glb` (586 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: ROUNDED WET-SAND BOULDER WITH TIDE POOL on top.

Dimensions: 1.5 m tall, ~1.8 m wide. Smooth rounded dark grey
boulder (#5a6060) with a shallow flat pool carved into the top
(~0.6 m diameter, turquoise water #40c0c0) and a small orange
starfish inside.
```

### `seashell_scatter.glb` (541 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
All shells merged. Target < 1500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: CLUSTER OF 5–6 SEASHELLS.

Dimensions: 0.3 m cluster footprint. Mix of conch (spiral), scallop
(fan), and snail shells. Warm whites and soft pinks (#f0e0d0,
#f09a9a). Scattered tightly around a shared centre.
```

### `starfish_decor.glb` (478 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 800 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: FIVE-POINT ORANGE STARFISH LYING FLAT.

Dimensions: 0.4 m wide, 0.05 m tall (flat on the ground). Bright
coral orange (#f07040), subtle dot texture on top.
```

---

# Pack 5 (extra) — KITSUNE SHRINE (🦊, Kurama)

## Files to generate

- `public/images/skyboxes/kitsune_shrine.png` (2048×1024)
- `public/images/arena-ground/kitsune_shrine.png` (1024×1024)
- 7 GLB props:
  - `torii_gate_large.glb`
  - `torii_gate_small.glb`
  - `stone_lantern.glb`
  - `stone_lantern_small.glb`
  - `bamboo_cluster.glb`
  - `sakura_tree.glb`
  - `kitsune_statue_white.glb`

Palette: vermillion crimson (#c8321a → #8a1a0a), gold (#d9b060),
cherry-blossom pink (#f8c8d8 → #f090b0), warm stone beige (#e8d8b8
→ #b89868), mossy green (#4a6a38), bamboo green (#6aa84a), dusk
lavender sky (#a888c8).

## Skybox prompt

```
Equirectangular 360° panoramic skybox, 2048×1024 px, seamless
horizontal tile.

Subject: Japanese sacred mountain at magic-hour dusk. Serene,
slightly mystical. Looking outward across a misty valley.

Sky: soft gradient from dusty lavender-violet (#8868a8) at the top
→ warm pink-peach (#f0a8b0) at the horizon. Thin warm cloud wisps
in cream-pink. Faint drifting sakura petals as tiny sparse pink
dots scattered across the sky.

Horizon: sacred mountain silhouette (Fuji-ish symmetric cone) at
~25 % of height, slightly off-centre, muted lavender. No moon, no
stars, no text, no visible civilization.

Painterly cartoon, bold colour regions, no photoreal detail.

Technical: 2:1, seamless, no watermark, fully opaque.
```

## Ground texture prompt

```
Seamlessly tileable 1024×1024 PNG, cartoon stylised, flat lighting.

Subject: Japanese shrine path. Pale grey-beige stone pavers
(#e8d8b8 → #c8b898) with gaps filled by dark mossy green (#4a6a38).
A few scattered pink sakura petals (#f8c8d8, #f090b0) fallen across
the stones. Tile should read as a sacred path when tiled.

Top-down, matte, even brightness. No characters, no text.

Technical: fully seamless, 1024×1024, no alpha.
```

## Prop prompts

### `torii_gate_large.glb` (700 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
bold outlines, vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 3500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: LARGE RED JAPANESE TORII GATE (shrine entrance).

Dimensions: 5.5 m tall, 3.5 m wide footprint, 0.4 m pillar diameter.
Two vermillion red vertical pillars (#c8321a), two horizontal
crossbeams at top, the topmost one curves upward at both ends.
Small gold rings around each pillar base. Subtle weathering.
```

### `torii_gate_small.glb` (569 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SMALLER TORII GATE (slimmer variant).

Dimensions: 3.0 m tall, 2.0 m wide. Same vermillion (#c8321a)
structure as the large torii but slimmer pillars (0.25 m). For
clustering in rows as a Fushimi-Inari style tunnel.
```

### `stone_lantern.glb` (637 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: STONE ISHIDORO LANTERN (Japanese pagoda lantern).

Dimensions: 2.2 m tall. Square stone base, cylindrical pillar,
lantern chamber with 4 small rectangular window slits, pagoda-
style roof cap on top. Weathered pale stone (#e8d8b8 → #b89868),
moss accents (#4a6a38) in seams.
```

### `stone_lantern_small.glb` (505 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 1500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SHORTER ISHIDORO LANTERN.

Dimensions: 0.8 m tall. Same stone pagoda-lantern shape as the
large version but miniature, for scatter along paths.
```

### `bamboo_cluster.glb` (588 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT separate stalks. Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: TALL BAMBOO CLUSTER.

Dimensions: 3.5 m tall, 1.0 m wide footprint. 5–7 bamboo stalks
of varied heights, bright green (#6aa84a → #5a8a3a), segmented
rings visible, sparse leaves near the top only.
```

### `sakura_tree.glb` (608 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Do NOT separate blossoms. Target < 3000 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: CHERRY BLOSSOM (SAKURA) TREE in full bloom.

Dimensions: 3.8 m tall. Dark gnarled brown-black trunk (#3a2a1a),
wide round-ish canopy fully covered in soft pink-white blossoms
(#f8c8d8 → #f0a8c0). Iconic sakura silhouette.
```

### `kitsune_statue_white.glb` (664 chars)

```
For "Bichitos Rumble" cartoon arena brawler — chunky chibi style,
vibrant saturated colours, NO realism.
CRITICAL GEOMETRY CONSTRAINT: single merged mesh with ONE material.
Target < 2500 triangles.
Output: GLB, Y-up metres, 1024px PBR baked, metalness ≤ 0.3, no
skeleton, no lighting. Origin at base centre, Y=0.

Generate: SEATED FOX (KITSUNE) GUARDIAN STATUE.

Dimensions: 1.6 m tall. Pale weathered stone (#e8d8b8) in sitting
pose, big pointed ears, long tail curled forward around the feet.
A single coloured accent: red cloth bib (#c8321a) around the neck.
Cute but solemn expression. Inari shrine guardian style.
```

---

# Integration — what happens when each pack lands

1. Drop the PNGs in their target paths (`public/images/skyboxes/<pack>.png`
   and `public/images/arena-ground/<pack>.png`).
2. Drop the raw GLBs in `public/models/arenas/_raw/<pack>/` (gitignored).
3. Run `node scripts/optimize-arena-props.mjs public/models/arenas/_raw/<pack> --pack <pack>`.
4. Inspect output sizes; anything over ~5 MB gets a run of
   `scripts/compress-arena-textures.mjs public/models/arenas/<pack> --max-size 512`.
5. Optional — anything still over budget can be sent through
   `scripts/aggressive-simplify.mjs` with `--ratio 0.002 --error 0.3`.
6. Once all 4 packs are in, the next dev block is the **loader**:
   `src/arena-decorations.ts` reads `packId` from the seed or URL,
   loads the GLBs from `public/models/arenas/<pack>/`, swaps the
   skybox and the ground texture on the arena fragments, and reads
   placement JSON for each prop's `{ radius, angleDeg, rotY, scale }`.
